import org.junit.Assert.assertEquals
import org.junit.Test

class ExampleUnitDataCallback {

    @Test
    fun addition_isCorrect() {
        assertEquals(4, 4)
        val a = "+62"
        println(a.length)
    }
}